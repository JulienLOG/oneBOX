import styles from "../assets/styles/pages/followPage.module.scss";

export default function FollowPage({ children }) {

    return (
        <div className={ styles.followPage }>{ children }</div>
    );
};